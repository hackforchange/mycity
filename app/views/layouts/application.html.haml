!!!html
%html{ :lang => "en" }
  %head
    %title MyCity - It's your city, fix it.
    %meta{ :'http-equiv' => "X-UA-Compatible", :content => "chrome=1" }
    %meta{ :charset => "utf-8" }
    = csrf_meta_tag
    = stylesheet_link_tag :all
    = javascript_include_tag :defaults
    = yield :head
  %body{:class => params[:controller].parameterize}
    - if notice && !notice.blank?
      .notices
        .notice= notice

    #main-container
      #header
        = image_tag 'header/logo.png', alt: 'MyCity'
        .city San Francisco

        = image_tag 'header/question.png', alt: "What's About My City?"

      = yield

      #footer
        %p designed, sliced, and coded by Team Awesome.
